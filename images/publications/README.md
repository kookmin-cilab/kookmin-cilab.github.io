Place publication thumbnails in this folder.

Supported formats on the publications page include `.gif`, `.jpg`, `.jpeg`, and `.png`.

Example usage in `_data/publist.yml`:

```yml
- title: "Example Paper"
  thumbnail: /images/publications/example.png
```
