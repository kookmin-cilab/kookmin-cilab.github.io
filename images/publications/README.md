Place publication thumbnails in this folder.

Example usage in `_data/publist.yml`:

```yml
- title: "Example Paper"
  thumbnail: example.png
```

If you use only a filename, the page automatically prepends `/images/publications/`.
Absolute paths and external URLs still work.
